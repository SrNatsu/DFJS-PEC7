import { NameArticleValidator } from './name-article-validator';

describe('NameArticleValidator', () => {
  it('should create an instance', () => {
    expect(new NameArticleValidator()).toBeTruthy();
  });
});
